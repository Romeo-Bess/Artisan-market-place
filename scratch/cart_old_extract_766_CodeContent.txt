"document.addEventListener('DOMContentLoaded', () => {\n    // 1. Locate all add to bag buttons\n    const addButtons = document.querySelectorAll('button, a');\n    \n    // Find shopping cart elements in header to update badge\n    const cartIcons = document.querySelectorAll('.material-symbols-outlined');\n    let cartLink = null;\n    for (let icon of cartIcons) {\n        if (icon.textContent.trim() === 'shopping_cart' || icon.textContent.trim() === 'shopping_bag') {\n            cartLink = icon.parentElement; // Anchor or parent container\n            break;\n        }\n    }\n\n    // Set up badge element\n    let badge = null;\n    if (cartLink) {\n        // Ensure relative positioning\n        cartLink.style.position = 'relative';\n        badge = document.createElement('span');\n        badge.className = 'absolute -top-2 -right-2 bg-secondary text-white font-label-sm text-[10px] w-4.5 h-4.5 rounded-full flex items-center justify-center hidden border border-surface';\n        badge.style.width = '18px';\n        badge.style.height = '18px';\n        badge.style.lineHeight = '18px';\n        cartLink.appendChild(badge);\n    }\n\n    let cartCount = 0;\n    const updateCartBadge = () => {\n        cartCount++;\n        if (badge) {\n            badge.textContent = cartCount;\n            badge.classList.remove('hidden');\n        }\n    };\n\n    addButtons.forEach(btn => {\n        const innerText = btn.textContent.trim();\n        const hasCartIcon = btn.innerHTML.includes('add_shopping_cart') || btn.innerHTML.includes('add_shopping_bag') || btn.innerHTML.includes('shopping_cart');\n        const isCartBtn = innerText.toLowerCase().includes('add to bag') || innerText.toLowerCase().includes('add to cart') || (hasCartIcon && (btn.tagName === 'BUTTON' || btn.classList.contains('material-symbols-outlined')));\n        \n        // Skip main shopping cart buttons in header\n        if (btn.id === 'auth-person-btn' || btn === cartLink || (btn.tagName === 'A' && btn.href && btn.href.includes('checkout.html') &&
<truncated 1974 bytes>